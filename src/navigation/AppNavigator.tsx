import React from 'react';
import { View } from 'react-native';

// Minimal AppNavigator placeholder to satisfy imports.
// In an Expo Router project the router handles navigation via the /app directory.
// Replace this implementation with the real navigator if desired.

const AppNavigator: React.FC = () => {
  return <View style={{ flex: 1 }} />;
};

export default AppNavigator;
